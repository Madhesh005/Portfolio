Temperature Program:

HTML Code:


    <form>
        <h1>Temperature <br> Conversion</h1>
        <label for="textbox"></label>
        <input type="number" id="textbox" value="0"><br><br>
        <input type="radio" name="temp" id="ctof">
        <label for="ctof">Celsius -> Fahrenheit</label><br>
        <input type="radio" name="temp" id="ftoc">
        <label for="ftoc">Fahrenheit -> Celsius</label><br><br>
        <button id="submit" onclick="toconvert()">submit</button>
        <p id="result"></p>
    </form>


CSS Code:

body{
    font-family: Arial, Helvetica, sans-serif;
    background-color: hsl(0, 0%, 95%);
}

h1{
    color: blue;
}

form{
    background-color: white;
    text-align: center;
    max-width: 350px;
    margin: auto;
    padding: 10px 5px;
    border-radius: 10px;
    box-shadow: 5px 5px 15px hsla(0, 0, 0, 0.3);
}

label, button{
    cursor: pointer;
}

#textbox{
    text-align: center;
}

#result{
    font-weight: bold;
}

button{
    background-color: rgb(255, 73, 73);
    border: none;
    border-radius: 10px;
    padding: 5px 10px;
    font-weight: bold;
}

JS Code:

const textbox = document.getElementById(`textbox`);
const ctof = document.getElementById(`ctof`);
const ftoc = document.getElementById(`ftoc`);
const result = document.getElementById(`result`);
let temp;

function toconvert(){
    if (ctof.checked){
        temp = Number(textbox.value);
        temp = temp * 9 / 5 + 32;
        result.textContent = temp.toFixed(1) + `°F`
    }
    else if(ftoc.checked){
        temp = Number(textbox.value);
        temp = (temp - 32) * (5 / 9); 
        result.textContent = temp.toFixed(1) + `°C`
    }
    else{
        result.textContent = `Please select the conversion to convert`
    }
}